{
  "name": "@skillevolver/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "openai": "^4.80.0",
    "uuid": "^11.0.0",
    "@skillevolver/sandbox": "workspace:*",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
