# Task: jpg-ocr-stat

Extract text statistics from JPEG images using OCR.

## Input
- One or more JPEG images in `input/`

## Output
- JSON file `output/stats.json` with: { "total_chars": N, "total_words": N, "files_processed": N }

## Evaluation
Run `bash evaluate.sh` to check correctness.
